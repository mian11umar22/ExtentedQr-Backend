<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Employee Details - DotLabs HR</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 text-gray-800 font-sans">

    <!-- Header -->
    <header class="bg-white shadow-sm sticky top-0 z-10">
      <div class="max-w-6xl mx-auto py-6 px-4 flex justify-between items-center border-b border-gray-200">
        <h1 class="text-3xl font-extrabold text-blue-700 tracking-wide">DotLabs HR Portal</h1>
        <span class="text-sm text-gray-400 italic">Employee Report</span>
      </div>
    </header>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto mt-10 bg-white shadow-xl rounded-xl p-10 border border-blue-100">
      <h2 class="text-2xl font-bold mb-6 text-blue-600 border-b pb-2">👤 Employee Information</h2>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-8">
        <div>
          <p class="text-sm text-gray-500 mb-1">Full Name</p>
          <p class="text-lg font-semibold text-gray-800"><%= Name %></p>
        </div>

        <div>
          <p class="text-sm text-gray-500 mb-1">Email</p>
          <p class="text-lg font-semibold text-gray-800"><%= Email %></p>
        </div>

        <div>
          <p class="text-sm text-gray-500 mb-1">Department</p>
          <p class="text-lg font-semibold text-gray-800"><%= Department %></p>
        </div>

        <div>
          <p class="text-sm text-gray-500 mb-1">Employee ID</p>
          <p class="text-lg font-semibold text-gray-800">#<%= employeeId %></p>
        </div>
      </div>

      <!-- QR Code Section -->
      <div class="mt-12 text-center">
        <h3 class="text-md text-gray-500 mb-2 uppercase tracking-wider">Employee QR Code</h3>
        <div class="inline-block border border-gray-300 p-3 rounded shadow-md bg-white">
          <img src="<%= qrCode %>" alt="QR Code" class="w-36 h-36" />
        </div>
      </div>
    </main>

    <!-- Footer -->
    <footer class="mt-20 bg-white py-4 shadow-inner">
      <div class="max-w-6xl mx-auto text-center text-sm text-gray-400">
        &copy; <%= new Date().getFullYear() %> DotLabs HR • All Rights Reserved
      </div>
    </footer>

  </body>
</html>
